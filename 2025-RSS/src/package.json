{
  "name": "@talks/2025-RSS",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "slidev",
    "build": "slidev build --base /talks/RSS/ --out ../../dist/RSS --timeout 120000",
    "export": "slidev export --per-slide --output ../slides.pdf"
  },
  "dependencies": {
    "@talks/components": "workspace:*"
  },
  "devDependencies": {
    "slidev-theme-geist": "^0.8.1"
  }
}
